# CVG GeoServ Processor — Python dependencies
# =============================================================================
# Matches pyproject.toml / setup.py exactly.
# Install via:  pip install -e .          (recommended — uses pyproject.toml)
#   or:         pip install -r requirements.txt   (CI / Docker fallback)
# =============================================================================

# Core geospatial
numpy>=1.26
rasterio>=1.3
fiona>=1.9
shapely>=2.0
geopandas>=0.14

# Contour generation
scikit-image>=0.22

# CLI
click>=8.1

# Web / API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0

# Reporting (optional — falls back to plain-text if absent)
reportlab>=4.0

# System monitoring (optional but recommended)
psutil>=5.9

# Dev / testing
pytest>=8.0
pytest-cov>=5.0
